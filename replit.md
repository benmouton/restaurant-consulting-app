# The Restaurant Consultant

## Overview
The Restaurant Consultant is a full-stack web application designed to provide independent restaurant owners and operators with robust operational systems. It offers an expert operations consultant, a comprehensive framework of content, training templates, and staff management tools. The project aims to deliver repeatable systems to address operational challenges, foster discipline, enhance efficiency, and act as an invisible expert. Key capabilities include advanced staff scheduling, employee portal access, labor demand analysis, food costing, and an expert-driven leadership command center.

## User Preferences
Preferred communication style: Simple, everyday language.

## System Architecture

### Frontend
- **Framework**: React 18 with TypeScript
- **Routing**: Wouter
- **State Management**: TanStack React Query
- **Styling**: Tailwind CSS with shadcn/ui (New York style), Radix UI-based components
- **Animations**: Framer Motion
- **Build Tool**: Vite
- **Key Design Decisions**: Path aliases, Replit Auth integration, streaming chat with markdown.

### Backend
- **Runtime**: Node.js with Express
- **Language**: TypeScript with ES modules
- **API Style**: RESTful endpoints
- **Authentication**: Replit Auth (OpenID Connect) with Passport.js
- **Session Management**: PostgreSQL-backed sessions (`connect-pg-simple`)
- **Key Design Decisions**: Modular integration pattern, custom build scripts, production builds to `dist/`.

### Data Storage
- **Database**: PostgreSQL with Drizzle ORM
- **Schema**: Defined in `shared/schema.ts` and `shared/models/`, covering users, sessions, conversations, and restaurant management entities.

### Core Features & Implementations
- **Staff Scheduling**: Full dark-themed scheduling page (`scheduling.tsx`) with 4 tabs (Schedule/Staff/Positions/Announcements). Features: Labor Strip header (5 gold-bordered metric cards: Total Shifts, Scheduled Hours, Est. Labor Cost, % Coverage, Staff Scheduled), position-colored shift chips on week calendar, shift detail modal, cross-tab integration (dropdowns populated from Staff/Positions tabs with "add first" links), stagger animations. Two AI features: **Build Schedule** modal (natural language prompt generates structured shift suggestions as clickable chips with "Add to Schedule" / "Add All") via `POST /api/scheduling/build-schedule`, and **Labor Impact Analysis** slide-in panel (revenue target + ideal labor % inputs, returns projected cost, risk day, coverage gaps, 3 recommendations) via `POST /api/scheduling/labor-analysis`. Staff tab: horizontal cards with initials avatar, position, rate, shift count badge. Positions tab: color swatch picker (8 colors), staff count per position. Announcements tab: gold left accent, priority pills, audience badge, character count. All empty states have dashed border + icon + gold CTA button. CSS: `scheduleStaggerIn`, `shiftPopIn`, `goldPulseRing` keyframes in index.css. Employee portal with Stripe $5/employee checkout flow preserved.
- **Financial Insights**: Full dark-themed financial insights page (`financial.tsx`) with Financial Intelligence Strip (4 gold-bordered metric cards: Documents Uploaded, Document Types, Analyses Run, Last Upload), redesigned 2-tab bar (Documents/Analysis) with gold active state, Documents tab: gold-bordered drag-and-drop upload zone with format chips (PDF/CSV/Excel/Image), auto-detect doc type from filename, 2x2 document type card grid (P&L/Sales/Labor/Inventory) with status indicators + View/Replace/Remove actions, POS callout, document summary with health-colored metric cards + key concerns. Analysis tab: upload gate warning, styled chat with gold user bubbles (right-aligned) + dark analyst bubbles (left-aligned, gold streaming border + typing dots) + action tray (Copy/Save Insight/Follow Up/Share), context-aware question chips (5 doc-type states), Saved Insights collapsible panel (max 8, export all), Full Analysis structured results card (SVG health score donut with animated arc, top 3 alerts + top 3 wins, action items tappable checklist with "Save as Playbook" cross-module link, key metrics table with benchmarks + status icons), "Ask the Consultant" deep link. Server: `POST /api/financial/extract-text` endpoint for PDF/image/CSV text extraction (GPT-4o vision fallback for scanned PDFs + images). CSS: `financialDragPulse`, `financialScoreArc` keyframes in index.css. SSE streaming via `POST /api/financial/ask` with conversation persistence. Also includes food costing system with plate costing, ingredient memory, and historical tracking via `MarginStatusStrip` and `FoodCostCalculator`.
- **Subscription & Access Control**: Freemium tiers (Free/Basic/Pro) with domain-based feature gating and Role-Based Access Control (Owner, General Manager, Manager).
- **Consulting Engine**: Full dark-themed consultant page (`consultant.tsx`) with Session Intelligence Strip (4 gold-bordered metric cards: Questions Asked, Domains Covered, Avg Response Time, Session Started), 12 Domain Tag Chips (Kitchen Ops, HR & Docs, Scheduling, Training, Financials, Reviews, SOPs, Crisis, Facilities, Social Media, Menu, Inventory) that inject domain context into API calls, Smart Prompt Starters (2x2 grid, domain-specific, tap populates input), redesigned chat bubbles (user = right-aligned gold tint with domain badge, consultant = left-aligned dark card with gold streaming border + 3-dot typing indicator), Action Tray on consultant messages (Copy/Pin/Regenerate/Share), Pinned Answers collapsible panel (max 4, tap scrolls to message), Response Mode Toggle (Quick/Full/Checklist), Suggested Follow-Up chips (domain-specific, 3 per response), conversation history sidebar (dark theme), empty state with chef hat icon. Input row: auto-expand textarea, gold focus ring, character counter >80 chars, gold send button, tap-hold clear, image attachment. CSS: `consultantTypingDot`, `goldStreamBorder`, `goldGlowPulse` keyframes in index.css. SSE streaming via `POST /api/consultant/ask` with conversation persistence.
- **Operational Command Centers**:
    - **Leadership**: Daily task management, crisis guidance, progress tracking.
    - **Crisis Management**: `CrisisReadinessStrip` and `CrisisResponseEngine` with scenario selection, severity triage, phase tracking, and post-crisis debriefing. Includes an "Owner Overwhelmed" mode.
    - **Facility**: `AssetHealthStrip` and `FacilityCommandCenter` for breakdown logging, PM scheduling, equipment management, and vendor tracking.
    - **Kitchen**: `KitchenStatusStrip` and `KitchenComplianceEngine` for real-time readiness, alerts, quick logging, debriefing, and coaching.
- **Content & Training**: Full dark-themed training templates page (`templates.tsx`) with Training Intelligence Strip (4 gold-bordered metric cards: Roles with Manuals, Handbook Status, Staff Assigned, Handbook Policies), redesigned 7-tab bar (Server/Kitchen/Host/Bartender/Manager/Busser/Handbook) with "Soon" badges on Coming Soon tabs, Coming Soon tabs have Quick-Start Outline generator (streaming training outline via `POST /api/training/generate`), Server/Kitchen TOC left panel with color-coded type badges (overview=gold, procedure=blue, policy=amber, quiz=green), content right panel with breadcrumb + gold-outlined key point chips + action tray (Copy/Edit Notes/Share/Print/Customize), Edit Notes saves to localStorage per template, Customize slide-in panel (380px right, streaming customization via consultant API, Replace Section + Add as Notes), Employee Training Progress with dark-themed accordion + assignment cards with gold progress bars + day-by-day checkboxes, cross-tab integration. HandbookBuilder (`HandbookBuilder.tsx`) with dark sub-tabs (Setup/Policies/Preview), gold focus rings on all inputs, completeness progress bars per section, custom policy management with localStorage, Preview with gold-bordered white card + Download PDF + Copy All Text buttons.
- **Labor Demand Engine**: `StaffingMetricStrip` and `LaborDemandEngine` for labor forecasting, staffing recommendations, and cost analysis.
- **HR Documentation Engine**: `HRStatusStrip` and `HRComplianceEngine` for progressive discipline tracking, policy management, and HR records viewing.
- **Living Playbooks**: Full dark-themed playbooks page (`playbooks.tsx`) with Playbook Intelligence Strip (4 gold-bordered metric cards: Total Playbooks, Categories Covered, Roles Covered, Last Created), redesigned left panel with category color-coded playbook cards (8 category colors), role filter + category filter + category chip quick-filter + quick-sort bar (Recent/A-Z/Category/Role), Starter Ideas panel (12 pre-built playbook ideas with one-tap prefill), Create modal with Context field (restaurant-specific context injection), Assist toggle, gold-selected mode cards with checkmark. Right panel: playbook viewer with breadcrumb + action tray (Run/Copy/Share/Print/Edit/Delete), content rendering by mode (checklist with tappable checkbox items + gold progress bar, step-by-step with gold numbered circles, deep procedure with gold section headers), Run Mode for checklists (large tappable rows, live progress bar, timer with start/pause/reset, completion banner with reset), Improve This Playbook collapsible panel (streaming regeneration via `POST /api/playbooks/improve`, Replace Playbook + Save as New Version). Playbook generation via `POST /api/playbooks/generate` SSE streaming. Schema: `content` and `context` fields added to playbooks table. CSS: `playbookStaggerIn`, `playbookCheckBounce`, `playbookStrikethrough`, `playbookCompletionSlide` keyframes in index.css.
- **Employee Handbook Builder**: Customizable handbook generation.
- **Reviews & Reputation**: `ReputationStatusStrip` and `ReviewResponseGenerator` for AI-powered review responses, including OCR for review text.
- **Social Media**: `SocialPerformanceStrip` (5 gold-bordered metric cards: Posts This Month, Channels Active, Scheduled, Last Post, Best Time Today) and `SocialPostBuilder` with 6 tabs (Create, Schedule, Channels, Holidays, Sent, Voice). Features: gold shimmer composer card, character count with platform-specific limits (280 X, 2200 Instagram, amber at 80%, red at 100%), 2 new Post Generator fields (Promotion/Discount text + Call to Action dropdown injected into prompt), Holiday chip prefill (sets Post Type + Event Name + navigates to Create tab), Voice Preview section (generates sample post in configured brand voice via `/api/social-media/voice-preview`), failed post inline explanation on Sent tab, Schedule empty cell click navigates to Create with date prefilled, dark-themed cards across all tabs. CSS: `socialShimmer` keyframe in index.css.
- **Dashboard / Landing Page**: Full dark-themed dashboard (`dashboard.tsx`) with upgraded greeting section (muted gray + bold white name split, gold crown icon for Owner, gold italic contextual subtitle, quick stats row with tier/role/restaurant), Operator Command Strip (5 gold-bordered live status cards: Tonight's Priority, Active Staff, Open Issues, Playbooks Built, Consultant Sessions — all clickable with navigation), Dynamic Priority Engine (6 time-of-day slots with context-specific subtitles, gold "Tonight" badge with pulse animation, day-of-week overrides for Fri/Sat/Monday), reordered domain tiles (Kitchen→Cost→Staffing→Scheduling→Service→HR→Training→SOPs→Reviews→Crisis→Facilities→Social→Leadership), Staff Scheduling inserted as tile #4 in main grid (separate bottom card removed), tile visual overhaul (gold icons, description, arrow with hover shift, gold border on hover, recently-visited gold dots via localStorage, priority highlight with gold top border + "Tonight" badge), ALSTIG INC App Suite Banner (4 app cards: Restaurant Consultant "You're here" badge, Review Responder, ChefScale, MyCookbook with "Open App" CTAs + iOS deep link fallback), Consultant bottom card (gold top border, 3 sample question chips that navigate to /consultant with prefilled prompt), staggered entrance animations via `playbookStaggerIn`, `tonightBadgePulse` keyframe in index.css, mobile responsive (command strip horizontal scroll, 1→2→4 col grid breakpoints, banner 2x2 on mobile).
- **Marketing & Onboarding**: Conversion-optimized landing page and multi-step onboarding wizard.

## External Dependencies

### Third-Party Services
- **Replit Auth**: User authentication.
- **OpenAI API**: AI capabilities via Replit AI Integrations.
- **PostgreSQL**: Database hosting.
- **Stripe**: Subscription management.
- **Meta (Facebook/Instagram) API**: Direct social media posting.
- **Google Business Profile API**: Direct social media posting.

### Capacitor (iOS Native)
- **Native Plugins**: @capacitor/browser, @capacitor/preferences, @capacitor/push-notifications, @capacitor/camera, @capacitor/haptics, @capacitor/share, @capacitor/network, @capacitor-community/apple-sign-in, capacitor-native-biometric, @capacitor-mlkit/text-recognition.